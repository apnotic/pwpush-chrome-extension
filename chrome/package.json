{
  "name": "@apnotic/password-pusher-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for Password Pusher APIv2 instance discovery and connectivity.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "node scripts/build.mjs",
    "validate:manifest": "node scripts/validate-manifest.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:ci": "npm run validate:manifest && npm run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "vitest": "^4.1.5"
  }
}
